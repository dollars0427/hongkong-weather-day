import fetch from 'node-fetch';

const weather = {
  fetchWeather(apiUrl, date){
    return new Promise((resolve, reject) => {
      const fullUrl = `${apiUrl}`;
    });
  },
};

export default weather;
